{
  "name": "@stakewars/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build ./...",
    "lint": "go vet ./...",
    "format": "gofmt -w .",
    "format:check": "test -z \"$(gofmt -l .)\"",
    "test": "go test ./..."
  }
}
